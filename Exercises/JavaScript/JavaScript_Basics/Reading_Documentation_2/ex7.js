// How many arguments does the Array.prototype.join() method expect? What happens if you call it with less or more arguments?

// no arguments needed - it defaults to an empty string if no arg given
// if more than one arg is given, it will ignore all other args except the first
